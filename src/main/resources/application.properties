spring.application.name=confirmation-system

# Use the database name we just created
spring.datasource.url=jdbc:mysql://localhost:3306/delivery_confirmation_v2?createDatabaseIfNotExist=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=yosoyfiesta2019

# This tells Hibernate to look at your @Entity classes and build the tables
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Add this to ensure MySQL 8+ compatibility
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect